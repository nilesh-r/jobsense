# JobSense AI Frontend
This is the Next.js frontend for JobSense AI.

## Setup

1. Make sure you install the dependencies from the root directory by running `npm run install:all`.
2. Configure `.env.local` using `.env.example` as a template.
3. Start the application with `npm run dev`.
